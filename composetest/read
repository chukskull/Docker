services:
  proxy:
    build: ./proxy
    networks:
      - frontend
  app:
    build : ./app
    networks:
      - frontend
      - backend
  db:
    image: postgress
    networks:
      - backend
networks:
  frontend:
   driver: custom-driver1 
  backend:
    driver: custom-driver2
    driver_opts:
      foo: "1"
      bar: "2"


      define( 'DB_NAME', 'mariadb' );

/** Database username */
define( 'DB_USER', 'sngt' );

/** Database password */
define( 'DB_PASSWORD', 'fes123' );

/** Database hostname */
define( 'DB_HOST', 'localhost' );
